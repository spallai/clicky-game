import React from "react";
import "./style.css";

const Header = () => (

    <div>
	<header className = "header">
		<h1>Valentine's Clicky Game!</h1>
		<h2>Click on any image but not more than once. Click all 12 pictures, and you win.</h2>
	</header>
    </div>
);

export default Header;